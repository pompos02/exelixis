# Caddyfile for Subdomain Routing with WebSocket Support

# Use a wildcard for the port to handle all our subdomains
:8000 {
    # Route requests for inventory.exelixis.local to the inventory app
    @inventory host inventory.exelixis.local
    handle @inventory {
        reverse_proxy 127.0.0.1:4001 {
            header_up Host {host}
            header_up X-Real-IP {remote_host}
            header_up X-Forwarded-For {remote_host}
            header_up X-Forwarded-Proto {scheme}
        }
    }

    # Route requests for orders.exelixis.local to the orders app
    @orders host orders.exelixis.local
    handle @orders {
        reverse_proxy 127.0.0.1:4000 {
            header_up Host {host}
            header_up X-Real-IP {remote_host}
            header_up X-Forwarded-For {remote_host}
            header_up X-Forwarded-Proto {scheme}
        }
    }

    # Route requests for auth.exelixis.local to the auth app
    @auth host auth.exelixis.local
    handle @auth {
        reverse_proxy 127.0.0.1:4003 {
            header_up Host {host}
            header_up X-Real-IP {remote_host}
            header_up X-Forwarded-For {remote_host}
            header_up X-Forwarded-Proto {scheme}
        }
    }

    # Optional: Add a default fallback or a root domain handler
    # handle {
    #   respond "Not Found" 404
    # }
}
